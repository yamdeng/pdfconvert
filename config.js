"use strict";

const config = {};

/* 경로는 window, mac의 폴더 구분자가 틀림 */

// oracle client install path
config.ORACLE_CLIENT_PATH = "C:\\instantclient_21_9";

// 쿼리 페이징 size
config.PAGE_SIZE = 10;

// session_id
config.SESSION_ID = 'E8C353DFF5A9F303B234DDD4A2A31922';

// jobMaxCount = 100;
conifg.JOB_MAX_COUNT = 100;

module.exports = config;
